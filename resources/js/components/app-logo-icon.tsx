export default function AppLogoIcon({className}: {className: string}) {
    return (
        <img
            className={className}
            src='/logo.svg'
            alt='Logo CM LABS'
        />
    );
}
